#include <iostream>
#include <string>
int main()
{
    std::string sampleOutput;
    sampleOutput = "Output from the test program: Test program to see if createprocess works";
    std::cout << sampleOutput;
    return 0;
}